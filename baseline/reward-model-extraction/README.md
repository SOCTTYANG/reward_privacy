# Reward-model extraction baselines

- `baseline1/`: MiniLLM reverse-KL reward-model extraction baseline.
- `baseline2/`: MiniPLM reward-model difference-sampling baseline and launcher.

The shared scoring and evaluation code remains in
`reward-model-extraction/`.
