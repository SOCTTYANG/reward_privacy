# Reward-model extraction defense

This directory contains record-level DP-SGD training for LoRA reward models and
the corresponding launch scripts. The scripts call shared scoring, extraction,
and evaluation code from `reward-model-extraction/`.
